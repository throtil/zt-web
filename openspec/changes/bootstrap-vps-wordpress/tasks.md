Чем закрытые задачи проверены и что выяснилось по ходу — в `implementation-notes.md`.

## 1. Инвентаризация сервера

- [x] 1.1 Снять список слушающих портов: снаружи только SSH на 22; Amnezia на UDP 43421; 80 и 443 свободны
- [x] 1.2 Снять список контейнеров и сетей Docker: один контейнер `amnezia-awg`, сеть `amnezia-dns-net` без контейнеров
- [x] 1.3 Сохранить правила пакетного фильтра как точку отката: 47 правил, копия в `/root/iptables.backup`
- [x] 1.4 Зафиксировать ресурсы: 981 МБ памяти без swap, диск 20 ГБ, свободно 15 ГБ; ufw не активен
- [x] 1.5 Записать результаты инвентаризации в `design.md` → Context
- [x] 1.6 Перенести копию `/root/iptables.backup` за пределы сервера
- [x] 1.7 Проверить с сервера исходящий доступ к Let's Encrypt и к Backblaze B2

## 2. Подготовка хоста

- [x] 2.1 Создать файл подкачки 2 ГБ, включить его и добавить в автозагрузку — до подъёма стека
- [x] 2.2 Задать умеренное значение `vm.swappiness`, чтобы подкачка служила запасом на пик, а не использовалась постоянно
- [x] 2.3 Создать непривилегированного пользователя с доступом к Docker, настроить вход по SSH-ключу
- [ ] 2.4 Отключить вход по SSH с паролем и вход под root, проверив подключение по ключу до применения изменений
  - **Пропущено решением владельца 6 августа 2026.** Парольный вход и вход под root на тестовой машине остаются. Причина отказа — не техническая: владелец не захотел закрывать себе привычный путь на чужую по сути машину, где живёт Amnezia. Следствия и что делать на целевой машине — в `implementation-notes.md`; открытый парольный вход записан в `docs/limitations.md`.
- [x] 2.5 Docker не устанавливать — он уже есть; при недостаточной версии плагина Compose обновить только его, проверив, что контейнер Amnezia продолжает работать
- [x] 2.6 Файрвол не настраивать: ufw не включать, правила Amnezia не трогать — порты 80 и 443 свободны и не требуют изменений
- [x] 2.7 Убедиться, что порт БД не отвечает с внешнего хоста (спека `container-stack`)
- [x] 2.8 Проверить работоспособность VPN-подключения Amnezia после изменений — отдельной проверкой, а не по факту доступности сайта
- [x] 2.9 Зафиксировать шаги 2.1–2.6 как скрипт или пошаговый документ в репозитории, воспроизводимый на чистом сервере

## 3. Описание стека

- [x] 3.1 Написать `docker-compose.yml`: сервисы WordPress (образ с Apache), MariaDB, Caddy; отдельная именованная сеть, не сеть по умолчанию; наружу публикуются только порты сайта
- [x] 3.2 Завести именованные тома: данные MariaDB и каталог WordPress; порт MariaDB не публиковать
- [x] 3.3 Добавить проверки готовности MariaDB и зависимость WordPress от её готовности (сценарий «база инициализируется дольше приложения»)
- [x] 3.4 Урезать MariaDB под 981 МБ: минимальный пул буферов, отключённая схема производительности, минимум соединений; пометить значения как уступку тестовой машине
- [x] 3.5 Смонтировать из рабочей копии только свой код — child-тему и mu-plugin — в их подкаталоги, не весь каталог WordPress
- [x] 3.6 Написать `Caddyfile`: приём на 80/443, редирект HTTP→HTTPS, проксирование на WordPress, передача реального IP посетителя и признака HTTPS
- [x] 3.7 Задать предел размера тела запроса одной переменной и подставить её во все три места: Caddy, настройки PHP, настройки WordPress
- [x] 3.8 Задать адрес сайта константами из окружения, а не значением в базе данных, чтобы смена IP на домен была изменением переменной
- [x] 3.9 Проверить: стек поднимается на сервере одной командой, WordPress отвечает на IP, установка проходит
- [x] 3.10 Проверить, что контейнер Amnezia продолжает работать после подъёма стека сайта, и что сети не конфликтуют

## 4. Замер обработки изображений (не блокирует остальное)

Выполняется в любой момент после группы 3. Результат влияет только на выбор памяти целевой машины и на решение медиа-итерации, оставлять ли обработку на сервере. Набор создаваемых размеров окончательно определится вместе с вёрсткой темы — здесь берётся черновое значение.

- [x] 4.1 Настроить обработку через GD с явным ограничением памяти, ImageMagick не использовать
- [ ] 4.2 Загрузить фотографию максимального размера и проверить, что превью созданы, а ни один процесс не убит по нехватке памяти (спека `container-stack`)
- [ ] 4.3 Повторить на серии из нескольких крупных файлов подряд, проверив, что сервисы остаются работоспособны
- [ ] 4.4 Проверить, что при неудаче обработки редактор получает сообщение об ошибке, а не пустую запись в медиабиблиотеке
- [ ] 4.5 Записать в `design.md` фактический результат: до какого размера файла обработка проходит на 981 МБ с подкачкой
- [ ] 4.6 По результату определить объём памяти целевой машины и зафиксировать в `design.md`; скорость обработки на тестовой машине в выводы не переносить

## 5. Режим работы на IP до появления домена

- [x] 5.1 Проверить, поддерживает ли Caddy запрос профиля `shortlived` для сертификата на IP-адрес
- [x] 5.2 Если поддерживает — включить HTTPS на IP и убедиться, что сертификат продлевается на сроке около шести суток; если нет — ограничить доступ к админке туннелем по SSH
  - Сертификат на IP выпущен живым обращением к Let's Encrypt: профиль `shortlived`, вызов http-01, срок 6 сут 15 ч. Продление **назначено** на 9 августа 2026 04:26 UTC (за 80 ч до истечения) — сам факт продления наступает позже этой работы и проверяется в тот день.
- [x] 5.3 Убедиться, что вход в админку ни в одном из вариантов не идёт по открытому HTTP
- [x] 5.4 Закрыть индексацию сайта поисковыми системами на период работы на IP
- [x] 5.5 Зафиксировать в репозитории, что до появления домена статьи не публикуются

## 6. Секреты

- [x] 6.1 Составить `.env.example` с полным перечнем переменных, назначением каждой и нерабочими заглушками
- [x] 6.2 Передать значения из `.env` каждому сервису поимённо, а не файлом целиком, чтобы доступы к объектному хранилищу не попадали в контейнер сайта; соли и ключи WordPress — переменными с префиксом `WORDPRESS_`, чтобы `wp-config.php` генерировался из окружения
- [x] 6.3 Применить обязательную подстановку для критичных переменных, чтобы отсутствующее или пустое значение останавливало запуск с указанием имени переменной
- [x] 6.4 Создать `.env` на сервере, выставить права «чтение и запись только владельцу»
- [x] 6.5 Проверить, что `.env` не попадает в индекс и не показывается как неотслеживаемое изменение
- [x] 6.6 Проверить сценарий отсутствующей и сценарий пустой переменной: запуск прерывается с именем переменной
- [x] 6.7 Описать в репозитории процедуру замены пароля БД и процедуру замены солей WordPress, выполнить обе один раз и убедиться в описанном поведении

## 7. Декларативный список плагинов

- [x] 7.1 Создать файл списка плагинов в формате `slug==версия`
- [x] 7.2 Написать скрипт сверки: вызывает wp-cli во временном контейнере, перечисляет лишние, отсутствующие и отличающиеся по версии плагины
- [x] 7.3 Добавить в скрипт режим приведения набора в соответствие со списком
- [x] 7.4 Проверить оба сценария спеки: плагин добавлен в список и устанавливается при деплое; плагин установлен из админки в обход списка и обнаруживается сверкой

## 8. Разделение прав редактора и разработчика

- [x] 8.1 Создать отдельные учётные записи: администраторскую для разработчика и редакторскую для редактора; общая учётная запись не используется
- [x] 8.2 Назначить редактору встроенную роль `editor`, не административную с урезанием прав плагином
- [x] 8.3 Отключить встроенный редактор файлов темы и плагинов для всех учётных записей, включая администраторские
- [x] 8.4 Задать роли, права и отключение редактора файлов кодом или скриптом в репозитории, чтобы они воспроизводились при пересоздании сервера
- [x] 8.5 Проверить доступное сейчас от лица редактора: создание и публикация статьи, массовая загрузка фотографий, категории и метки — без сообщений о недостатке прав. Проверка шаблона обзора невозможна до его появления и переносится в итерацию вёрстки
- [x] 8.6 Проверить запреты от лица редактора: установка плагинов, обновление ядра, управление пользователями — доступ не предоставляется
- [x] 8.7 Проверить, что раздел редактирования файлов темы недоступен даже под администратором
- [ ] 8.8 Сверить перечень выданных редактору доступов: только учётная запись WordPress, без SSH-ключа, прав в репозитории и ключей объектного хранилища
- [ ] 8.9 Проверить воспроизводимость: после пересоздания сервера из репозитория распределение прав то же, ручная донастройка не потребовалась

## 9. Локальная копия для разработки

- [x] 9.1 Написать override-файл для локального запуска: свои тома, публикация порта напрямую, без Caddy и TLS
- [x] 9.2 Проверить, что правка файла child-темы видна в локальном браузере без пересборки образа
- [x] 9.3 Проверить изоляцию: изменение данных в локальной копии не затрагивает сервер

## 10. Бэкап БД и восстановление

- [ ] 10.1 Создать бакет в Backblaze B2 под дампы БД, ключ доступа с правами только на этот бакет, добавить переменные в `.env.example` и `.env`
- [x] 10.2 Написать скрипт бэкапа: дамп средствами MariaDB из контейнера, проверка непустоты и целостности файла, локальная копия, выгрузка в B2
- [x] 10.3 Реализовать удаление копий старше срока хранения — строго после успешной проверки целостности нового дампа, чтобы неудачный дамп не вытеснял годные
- [x] 10.4 Добавить в скрипт проверку свободного места: при недостатке сообщать и не удалять имеющиеся годные копии
- [ ] 10.5 Поставить скрипт в cron на хосте, настроить сообщение о неуспешном запуске
- [x] 10.6 Написать скрипт проверки свежести последней годной копии, дающий явный отрицательный результат при превышении ожидаемого интервала
- [x] 10.7 Проверить сценарий неудачного дампа: подсунуть заведомо сбойный запуск и убедиться, что предыдущие копии сохранены, а сбой зафиксирован
- [x] 10.8 Описать процедуру восстановления с указанием ожидаемой длительности
- [ ] 10.9 Выполнить учебное восстановление на отдельной копии стека, не затрагивая рабочий сайт; сверить фактическую длительность с указанной

## 11. Деплой

- [x] 11.1 Склонировать репозиторий на сервер в рабочую копию
- [x] 11.2 Написать скрипт деплоя: проверка чистоты рабочей копии, отказ при локальных изменениях, переход на целевой коммит, применение к сервисам
- [x] 11.3 Добавить в скрипт создание свежего дампа перед деплоем, затрагивающим базу, с отказом начинать при неудаче дампа
- [x] 11.4 Добавить приведение набора плагинов к списку как шаг деплоя
- [x] 11.5 Добавить завершающую проверку: сервисы прошли готовность, публичная страница и админка отвечают без ошибки сервера; иначе деплой неуспешен
- [x] 11.6 Описать и проверить процедуру откката на предыдущий коммит без пересоздания томов
- [x] 11.7 Проверить сценарий спеки: деплой поверх сайта со статьями и изображениями сохраняет их и настройки
- [x] 11.8 Проверить сценарий спеки: ручная правка файла на сервере обнаруживается и не затирается молча

## 12. Домен и TLS

- [ ] 12.1 Зарегистрировать домен, выбрать каноническое имя
- [ ] 12.2 Направить DNS-запись напрямую на сервер, без проксирования
- [ ] 12.3 Задать домен переменной, перезапустить стек, убедиться в автоматическом выпуске сертификата
- [ ] 12.4 Проверить: HTTP отдаёт постоянный редирект на HTTPS, сертификат действителен, предупреждений браузера нет
- [ ] 12.5 Настроить редирект с неканонических имён на каноническое, проверить
- [ ] 12.6 Проверить, что сертификат переживает перезапуск стека без нового обращения к центру сертификации
- [ ] 12.7 Задать канонический адрес сайта в WordPress, проверить схему `https` во всех ссылках и ресурсах, отсутствие смешанного содержимого и отсутствие цикла редиректов при входе в админку
- [ ] 12.8 Проверить, что журналы приложения фиксируют IP посетителя, а не адрес прокси
- [ ] 12.9 Проверить загрузку файла ровно максимального размера и файла сверх предела: успех в первом случае, понятное сообщение об ошибке во втором

## 13. Включение Cloudflare

- [ ] 13.1 Перенести домен в Cloudflare как DNS-провайдера, оставив запись без проксирования
- [ ] 13.2 Включить проксирование и режим полной проверки сертификата origin
- [ ] 13.3 Повторить проверки: доступность по HTTPS, схема ссылок, отсутствие цикла редиректов, реальный IP посетителя за CDN
- [ ] 13.4 Измерить фактический предел размера загрузки через проксирование на реальном файле максимального размера; при недостаточности предусмотреть отдельное имя для админки без проксирования
- [ ] 13.5 Проверить прямое обращение к серверу в обход CDN с указанием канонического имени

## 14. Приёмка

- [ ] 14.1 Пройти сценарии спеки `container-stack`: подъём на чистом хосте, повторный запуск без потери данных, удаление и повторное клонирование рабочей копии, недоступность порта БД снаружи
- [ ] 14.2 Проверить чистоту репозитория после загрузки изображений, установки плагинов и обновления ядра
- [ ] 14.3 Пройти сценарии спек `secrets-management`, `db-backup`, `deploy`, `edge-tls`, `role-separation`, не покрытые проверками выше
- [ ] 14.4 Финальная проверка Amnezia: VPN-подключение работает, её контейнеры и правила не изменились относительно инвентаризации шага 1
- [x] 14.5 Зафиксировать в репозитории известные ограничения итерации: загрузки без резервной копии, ограниченная и общая с Amnezia ёмкость диска, сайт и VPN на одном IP
- [x] 14.6 Составить в репозитории отдельный перечень уступок тестовой машине — что именно урезано, почему и что пересмотреть на целевой машине
- [ ] 14.7 Заполнить фактические значения в открытых вопросах `design.md` и заархивировать изменение
