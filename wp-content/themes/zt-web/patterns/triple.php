<?php
/**
 * Title: Тройка фотографий
 * Slug: zt-web/triple
 * Categories: zt-web
 * Description: Камень и два снимка с микроскопа — три кадра в ряд на любой ширине экрана. Вставляется столько раз, сколько троек в обзоре.
 * Keywords: галерея, фото, тройка, микроскоп
 * Inserter: yes
 *
 * Вставляется копией: каждая тройка живёт своей жизнью в своей статье.
 *
 * Галерея пустая — редактор загружает в неё серию одной пачкой. Число
 * столбцов и зазор уже заданы, настраивать их не требуется. Порядок кадров
 * определяется именами файлов, соглашение об именах — в
 * docs/photo-conventions.md.
 *
 * `linkTo: none` здесь не оформление, а условие работы просмотра: ядро
 * открывает кадр на полный экран только у изображения без ссылки. Кадр со
 * ссылкой вместо просмотра уводит на страницу вложения, и сетка из трёх
 * кадров по сотне пикселей перестаёт показывать что-либо вообще.
 *
 * `imageCrop: false` выключает обрезку по той же причине, по которой в
 * style.css кадры выровнены по верху: обрезка идёт от центра, а надписи
 * нанесены на фотографии у краёв и срезались бы. Значение указано явно,
 * потому что по умолчанию в ядре обрезка включена, и разметка ниже обязана
 * совпадать с тем, что ядро соберёт из атрибутов: редактор сверяет их
 * посимвольно, и лишний или недостающий класс — это «Блок содержит
 * неожиданное или недопустимое содержимое», а не мелкое расхождение.
 *
 * @package zt-web
 */

?>
<!-- wp:gallery {"columns":3,"imageCrop":false,"linkTo":"none","className":"is-style-zt-triple"} -->
<figure class="wp-block-gallery has-nested-images columns-3 is-layout-flex wp-block-gallery-is-layout-flex is-style-zt-triple"></figure>
<!-- /wp:gallery -->
